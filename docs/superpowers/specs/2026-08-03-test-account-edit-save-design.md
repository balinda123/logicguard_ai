# 测试账号编辑保存设计

## 目标

修复测试账号编辑成功后仍展示旧表单数据的问题，并在保存成功后自动关闭编辑卡片。

## 行为

- 编辑保存时继续使用原测试账号 ID 调用更新接口。
- 更新成功后重新读取账号列表，确保页面展示持久化后的数据。
- 列表刷新成功后清空编辑状态并关闭编辑卡片。
- 更新或刷新失败时保留编辑卡片和用户输入，并显示错误信息，方便重试。

## 范围

仅调整 `TestAccountsPanel` 的前端状态管理和组件测试；后端更新接口已按账号 ID 持久化，无需修改。
