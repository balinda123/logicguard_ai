# 测试小助手使用指南

测试小助手是一款桌面端 AI 自动化测试与浏览器任务执行工具。用户用自然语言描述目标，系统通过大模型理解任务，并连接受控 Chrome/Edge 浏览器完成页面操作、用例回放、回归套件执行和测试报告生成。

> 说明：应用显示名已统一为“测试小助手”；内部数据目录、identifier 和部分历史存储键仍保留 logicguard 命名以兼容旧数据。

## 1. 使用前准备

### 1.1 运行环境

已安装版本通常内置运行所需的 Node.js 与浏览器自动化组件，普通用户只需要准备：

- Windows 或 macOS 桌面环境。
- Google Chrome 或 Microsoft Edge。
- 可用的大模型 API Key，例如 Gemini、DeepSeek、OpenAI、通义千问、Kimi、智谱、豆包，或 OpenAI Compatible 接口。
- 目标业务系统的测试账号。

开发运行时需要：

- Node.js 22.x。
- Rust stable。
- Chrome 或 Edge。

开发启动命令：

```bash
npm ci
npm run tauri dev
```

## 2. 首次启动与登录

1. 启动测试小助手。
2. 首次打开时，系统会要求创建本机管理员账号。
3. 输入用户名和密码后进入工作区。
4. 管理员可以在「系统设置」中创建普通用户、禁用用户或重置密码。
5. 后续每次打开应用，使用本机账号登录即可。

账号只用于本机应用隔离。每个用户的模型配置、模板、测试报告会按登录用户分开保存。

## 3. 配置 AI 模型

进入左侧「系统设置」，在「AI 模型接入配置」区域完成配置。

1. 选择模型提供商预设。
2. 确认或修改模型名称。
3. 如果使用 OpenAI Compatible 服务，确认 Base URL。
4. 输入当前用户自己的 API Key。
5. 点击「测试并保存配置」。
6. 看到连接成功后，即可执行智能任务。

API Key 会保存到系统安全凭据中：

- Windows：Credential Manager。
- macOS：Keychain。

API Key 不会写入 localStorage、SQLite、日志或测试报告。

## 4. 连接受控浏览器

测试小助手通过 Chrome DevTools Protocol 控制浏览器。进入「系统设置」中的「浏览器 CDP 控制」区域。

1. 保持默认 CDP 端口 `9222`，或改成未被占用的端口。
2. 点击「一键启动受控浏览器」。
3. 在新打开的浏览器中登录目标业务系统。
4. 如果业务系统需要短信、扫码、MFA 或 SSO，请先手动完成登录。
5. 回到测试小助手，点击顶部状态刷新或直接开始任务。

如果需要手动启动浏览器，可以使用类似命令：

```powershell
chrome.exe --remote-debugging-port=9222
```

建议使用「一键启动受控浏览器」，它会自动按当前配置启动可连接的浏览器实例。

## 5. 执行自然语言任务

进入左侧「任务控制台」。

1. 在输入框中描述要完成的操作。
2. 尽量写清楚目标页面、筛选条件、点击对象、期望结果。
3. 点击「开始 AI 智能执行」。
4. 右侧控制台会实时显示 Agent 的执行步骤和日志。
5. 执行成功或失败后，系统会保存一份测试报告。

示例任务：

```text
进入绩效管理系统，筛选 2026 年第二季度的待审批记录，打开第一条记录，检查员工姓名、部门和绩效等级是否展示完整。
```

更推荐的写法：

```text
当前浏览器已经登录绩效管理系统。请进入“绩效审批”页面，筛选周期为“2026 Q2”、状态为“待审批”，打开列表第一条记录，检查详情页是否包含员工姓名、部门、直属主管和绩效等级。不要提交审批，只做页面校验。
```

## 6. 使用测试设计生成并执行测试用例

进入左侧「测试设计」。页面按“需求来源 → 场景模板 → 测试用例 → 回归执行”组织。

### 6.1 从需求生成用例

1. 填写模块名称，例如「入职流程」。
2. 粘贴需求、验收标准或业务场景说明。
3. 点击生成按钮。
4. 系统会生成正常流程、边界值、空值异常、权限、重复提交和组合测试等用例。

### 6.2 人工确认

生成的用例默认是草稿状态。执行前需要人工确认：

1. 查看用例标题、前置条件、步骤、预期结果和风险点。
2. 确认测试数据是否为虚构数据。
3. 点击「人工确认」。

### 6.3 加入回归套件

1. 新建或选择一个回归套件。
2. 点击用例上的「加入当前套件」。
3. 后续可以单独执行用例，也可以一键执行整个套件。

### 6.4 执行用例

执行前请确保：

- 模型连接成功。
- 受控浏览器已连接。
- 浏览器已登录目标系统。
- 当前页面处于适合开始测试的位置。

点击「执行」或「一键执行套件」后，系统会生成确定性脚本并回放操作，最终写入测试报告。

## 7. 使用场景模板

进入左侧「测试设计」，在「场景模板」区域选择已有模板，或点击「从需求文档建模」进入全屏建模页。

模板适合复用固定流程，例如登录、表单填写、审批、查询筛选等。

### 7.1 使用已有模板

1. 在模板列表中选择一个模板。
2. 填写模板参数。
3. 如需复用多组数据，可保存为参数集。
4. 执行单个参数集，或批量执行多个参数集。

### 7.2 从页面或文档生成模板

1. 先在受控浏览器中打开目标页面。
2. 在全屏建模页中抓取当前页面文本，或粘贴需求文档内容。
3. 点击 AI 解析生成模板。
4. 检查生成的步骤、变量和标签。
5. 保存到模板库。

模板中的变量可以使用 `{{变量名}}` 占位。执行时，测试小助手会用参数值替换变量并驱动浏览器完成操作。

## 8. 查看测试报告

进入左侧「测试报告」。

报告页面提供：

- 总执行次数。
- 运行成功率。
- 平均响应耗时。
- Healer 自愈补救次数。
- 报告搜索和通过/失败筛选。
- 单次任务的详细日志、步骤成功比例、管理摘要和发版建议。

点击报告可进入详情页。详情页中可以复制原始任务意图，用于重新执行或复盘。

## 9. 数据安全建议

测试小助手默认使用严格脱敏模式，在发送给大模型前会处理页面文本、需求、用例和报告摘要中的敏感信息。

建议：

- 测试环境使用虚构员工、虚构手机号、虚构身份证号和虚构薪资数据。
- 不要在任务描述中粘贴真实个人隐私、生产账号密码或不可外发数据。
- 如必须处理真实数据，优先选择本地模型或私有化模型服务。
- 默认不要开启真实截图保存，截图可能包含员工姓名、薪资、身份证等敏感信息。

可在「系统设置」中切换数据安全模式：

- 严格脱敏：默认推荐。
- 本地模型优先：适合 Ollama 或内网模型。
- 云端模型增强：仅建议在确认无真实敏感数据时使用。

## 10. 常见问题

### 10.1 提示未配置 API Key

进入「系统设置」，重新填写 API Key，点击「测试并保存配置」。每个本机用户都需要配置自己的模型凭据。

### 10.2 模型连接失败

检查以下项目：

- API Key 是否正确。
- Base URL 是否包含正确路径，例如 OpenAI Compatible 通常以 `/v1` 结尾。
- 当前网络是否能访问对应模型服务。
- 模型名称是否真实存在。

### 10.3 CDP 连接失败

处理方式：

1. 确认已点击「一键启动受控浏览器」。
2. 确认端口没有被其他程序占用。
3. 关闭旧的受控浏览器后重新启动。
4. 如手动启动浏览器，确认启动命令包含 `--remote-debugging-port=9222`。

### 10.4 任务执行到一半失败

常见原因：

- 页面加载太慢。
- 目标元素文案变化。
- 当前账号权限不足。
- 业务系统弹出二次确认或登录过期。
- 任务描述过于模糊。

建议补充更具体的页面位置、筛选条件和预期结果后重新执行。

### 10.5 用例无法执行

确认用例已「人工确认」。草稿用例不能直接执行。

### 10.6 报告中没有新记录

确认任务已经执行结束。如果执行过程被终止或应用异常关闭，报告可能不会写入。也可以进入「系统设置」查看当前用户报告文件位置。

## 11. 推荐工作流

### 单次探索任务

1. 配置模型。
2. 启动受控浏览器。
3. 登录目标系统。
4. 在「任务控制台」输入自然语言任务。
5. 查看执行日志和报告。

### 回归测试

1. 在「测试设计」中粘贴需求。
2. 生成并人工确认用例。
3. 将核心用例加入回归套件。
4. 启动受控浏览器并登录测试环境。
5. 一键执行套件。
6. 在「测试报告」中查看通过率、失败原因和发版建议。

### 复用固定流程

1. 在「测试设计」的「场景模板」区域选择模板，或从需求文档建模生成模板。
2. 配置变量和参数集。
3. 批量执行参数集。
4. 查看每次执行的报告。
