# 测试小助手开发文档

> 本文描述当前已实现代码。构建操作见 [BUILDING.md](./BUILDING.md)，安装部署见 [零成本部署附录.md](./零成本部署附录.md)。

## 1. 产品边界

测试小助手是本机运行的桌面自动化测试工具：

- UI、用户登录、报告和浏览器执行均在本机。
- 推理使用用户自行配置的 Gemini 或 OpenAI Compatible 云模型。
- 浏览器通过本机 CDP 控制，不上传浏览器 Profile。
- 不依赖 PocketBase、Tailscale、Ollama 或自建后端。
- 当前支持 Windows x64；macOS 目标为 Apple Silicon 未签名内部包。

## 2. 系统架构

```text
React UI
  ├─ Dashboard：任务与 Stagehand Agent 执行
  ├─ TestCases：测试设计主向导，管理步骤状态、用例、套件和模板
  │    └─ RequirementModeler：需求 URL、关键词、抓取、建模和保存子向导
  ├─ Reports：按用户查看报告
  └─ Settings：模型、CDP、用户管理
        │ Tauri invoke / event
        ▼
Rust
  ├─ auth.rs：SQLite 用户、Argon2id、内存会话、系统凭据
  ├─ test_design.rs：系统、环境、测试设计、需求版本、生成批次、检查记录和回归配置
  ├─ llm.rs：Gemini / OpenAI Compatible 请求与 Prompt
  ├─ browser.rs：内置 Runtime、sidecar、Chrome CDP 和 Agent 事件
  └─ reports.rs：按用户保存报告
        │ 子进程
        ▼
Node.js 22 + sidecar/index.js
  └─ Playwright + Stagehand → Chrome / Edge CDP
```

安装版的 Node Runtime、sidecar 脚本和生产依赖位于 Tauri resources。开发版找不到内置资源时才允许使用项目 `sidecar/` 与系统 Node。

`TestCases` 是“需求来源 → 生成用例 → 检查确认 → 回归执行”四步主向导的所有者，负责当前步骤、解锁状态、需求与模块修订、测试用例、回归套件、默认/自定义模板及选择状态。步骤只能按前置条件解锁，但可以返回已完成步骤而不清空输入或生成状态；修改需求、模块或模板选择后会使生成结果过期并要求重新生成，不会删除已持久化用例。

`RequirementModeler` 仅作为 `TestCases` 的内联子组件存在，负责 HTTP(S) URL、可选关键词、页面抓取、可编辑正文、AI 模板草稿和保存状态。抓取严格按 `browserNavigate(url)` 后 `getPageContent(keyword)` 的顺序调用，以先完成受控 Chrome/Edge 导航再提取正文；组件通过 `onCancel()` 回到保留原状态的测试设计，通过 `onSaved(template)` 返回完整模板。`TestCases` 随后重新合并默认和自定义模板、自动选中新模板并进入生成步骤。

模板类型、本地持久化、默认/自定义模板合并和基于模板生成用例仍然保留；运行时没有独立模板路由、旧模板卡片列表、参数集配置页或旧模板直接执行工作区。

## 3. 登录与数据隔离

### 3.1 用户

用户表位于应用数据目录下的 `logicguard.db`：

| 字段 | 说明 |
| --- | --- |
| `id` | UUID |
| `username` | 不区分大小写、唯一 |
| `password_hash` | Argon2id 摘要 |
| `role` | `admin` 或 `user` |
| `disabled` | 禁用标记 |

首次启动只能创建一个管理员。管理员可以在系统设置中查看用户列表，创建、禁用普通用户和重置密码。用户列表返回启用/禁用状态，并预留权限列用于后续角色/功能授权。登录会话只保存在 Rust 进程内存中，应用退出后失效。

### 3.2 API Key

- Windows：Credential Manager
- macOS：Keychain
- service：`com.logicguard.ai`
- account：用户 UUID

前端只持有输入瞬间的密钥和 `credentialConfigured` 状态。后续 LLM 调用由 Rust 根据当前会话读取密钥；sidecar 需要密钥时由 Rust 临时注入子进程环境。

### 3.3 用户数据

| 数据 | 当前存储 |
| --- | --- |
| 用户账号 | SQLite |
| API Key | 系统凭据库 |
| 系统、环境、测试设计、需求版本、生成批次、检查记录和回归配置 | SQLite；系统和环境为管理员维护的全局配置，其余按用户 UUID 隔离 |
| 测试账号、账号组合、流程场景、运行、失败证据和缺陷草稿 | SQLite；新 scoped 入口按 owner、系统和环境校验，旧记录显式标记 legacy/unassigned |
| 报告 | 应用数据目录中的用户专属 JSON，临时文件后原子替换 |
| 测试用例/回归套件 | 正式测试设计写入走 SQLite；旧 `test_cases` / 回归套件 localStorage 仅为 Task 9 迁移前的 UI 兼容与历史读取入口 |
| 模板 | 以用户 UUID 命名空间隔离的 localStorage |
| 模型/CDP 配置 | 以用户 UUID 命名空间隔离的 localStorage |
| 数据安全模式 | 以用户 UUID 命名空间隔离的 localStorage，默认严格脱敏 |

这是本机应用级隔离，不承诺抵御拥有相同操作系统账号或管理员权限的攻击者。

系统设置页通过 `get_storage_locations` 展示当前用户的数据位置，并通过 `open_app_data_dir` 打开应用数据目录。普通用户可以看到自己的报告 JSON 路径；管理员还可以看到用户库位置。API Key 只显示系统凭据库 service/account，不显示明文。

## 4. 执行路径现状

当前任务控制台仍只暴露 Stagehand Agent 作为主执行路径。“测试设计”页面用于从需求来源或场景模板生成测试用例、人工确认、加入回归套件，并在执行已确认用例时优先使用 Script 生成与确定性回放。

### 4.1 Stagehand Agent

当前默认且唯一暴露的自然语言任务执行路径。Dashboard 调用 `browser_run_agent`，Rust 启动 sidecar `agent` 命令并将 `[AGENT_STEP]` JSON 事件转发到 UI。适合自然语言多步任务。需求建模子向导不调用 Agent；测试设计中的已确认用例按下一节的 Script 路径执行。

### 4.2 Script（保留备用，当前 UI 不可切换）

代码位于 `scriptGenerator.ts`、`scriptExecutor.ts` 和 `generate_test_script` 命令中。测试设计页面使用该路径执行已确认用例：若用例关联了场景模板，脚本生成意图会优先包含模板流程骨架，再叠加用例测试数据和预期结果；随后由 LLM 基于当前页面快照生成 `TestScript`，再由本地执行器按步骤确定性回放。Dashboard 仍不显示三模式切换入口。

### 4.3 Classic（保留备用，当前 UI 不可切换）

代码仍保留在 `executorEngine.ts`、`plan_task`、`generate_action` 和 `heal_step` 中。设计目标是 Planner 生成步骤，Generator 根据压缩 DOM 生成动作，失败后 Healer 重新决策；但当前 Dashboard 不显示 Classic 执行入口。

正式模式未配置 API Key 时直接中止并引导设置，不再自动生成模拟成功结果。

## 5. 浏览器与 sidecar

### 5.1 CDP

- 默认端口 `9222`，允许用户配置 `1024–65535`。
- 配置按登录用户持久化，所有桥接调用从同一配置读取。
- Chrome 使用独立 Profile，路径来自 Tauri app data directory。
- Windows 自动发现 Chrome / Edge 常用安装路径。
- macOS 自动发现 `/Applications` 和用户 Applications 下的 Chrome / Edge。

### 5.2 sidecar 协议

普通命令输出：

```json
{"ok":true,"data":{}}
{"ok":false,"error":"错误信息"}
```

Agent 过程事件：

```text
[AGENT_STEP]{"type":"action","description":"...","timestamp":"..."}
```

Rust 同时消费 stdout 和 stderr，防止 stderr 缓冲区写满导致子进程卡死。当前仅允许一个前台 Agent，终止命令按保存的 PID 结束该进程。

命令和环境变量详见 [sidecar/README.md](./sidecar/README.md)。

## 6. Tauri 命令

### 6.1 身份与凭据

- `auth_status`
- `initialize_admin`
- `login` / `logout`
- `list_users` / `create_user` / `disable_user`
- `reset_user_password`
- `save_api_key` / `credential_status`
- `get_storage_locations` / `open_app_data_dir`

### 6.2 模型

- `test_llm_connection`
- `plan_task`
- `generate_action`
- `heal_step`
- `generate_test_script`

前端模型提供商下拉使用预设形式维护，当前包含 DeepSeek、OpenAI、通义千问、Kimi、智谱、豆包、Gemini、Ollama 和自定义 OpenAI Compatible。预设会自动填充 `provider`、`model` 和 `base_url`，后端仍按 `gemini`、`openai_compat`、`ollama` 三类协议路由。

### 6.3 浏览器

- `browser_check_sidecar` / `browser_check_connection`
- `launch_chrome_cdp` / `get_chrome_path`
- `browser_get_snapshot` / `browser_get_page_content`
- `browser_click` / `browser_hover` / `browser_type`
- `browser_press` / `browser_navigate` / `browser_assert`
- `browser_act` / `browser_observe`
- `browser_run_agent` / `browser_terminate_agent`

### 6.4 报告

- `load_reports_from_file`
- `save_reports_to_file`

报告命令根据 Rust 当前会话决定用户，前端不能指定 owner ID。

### 6.5 状态流转测试

- `testing.rs` 管理测试账号元数据、账号组合、流程场景、运行、语义事件、失败证据和问题草稿。除管理员管理的脱敏账号元数据外，所有记录按当前用户的 `owner_id` 隔离。
- 真实测试账号用户名和密码只写入 Windows Credential Manager 或 macOS Keychain。SQLite 仅保存服务端生成的 `credential_ref` 与掩码账号名；前端没有读取凭据的命令。
- 自动登录由 Rust 读取系统凭据后临时注入 sidecar 环境；SSO/OTP 运行进入 `waiting_handoff`，由测试人员在浏览器完成后恢复同一个运行。
- 运行状态只能前进；业务断言失败才生成受控目录下的失败截图、失败证据和 `pending_confirmation` 问题草稿。技术阻断不生成问题草稿，也不截图。
- 执行中心展示语义事件；问题跟踪页管理确认、修复、验证和关闭，并只导出业务修复字段及失败证据相对路径。

### 6.6 系统级测试设计数据基础

- 系统：`list_systems` / `create_system` / `update_system`
- 环境：`list_system_environments` / `create_system_environment` / `update_system_environment`
- 测试设计：`list_test_designs` / `create_test_design` / `update_test_design`
- 需求版本：`list_requirement_versions` / `create_requirement_version`
- 生成批次：`list_generation_batches` / `create_generation_batch`
- 检查记录：`list_review_records` / `create_review`
- 回归配置：`get_regression_config` / `save_regression_config`

SQLite 新增 `systems`、`system_environments`、`test_designs`、`requirement_versions`、`generation_batches`、`review_records` 和 `regression_configs`。系统和环境是全局配置：登录用户可读取，只有管理员可创建或更新（包括禁用环境）；测试设计及其下属记录从 Rust 当前会话派生 owner，前端不能指定 owner ID。

前端 `testDesignBridge.ts` 已为上述命令提供 camelCase 强类型调用，并在 bridge 边界把 Rust nullable 字段归一化为可选字段；命令错误不做吞并或降级。正式测试设计的新写入应通过该 bridge 进入 SQLite，不应写入 `test_cases` localStorage。

环境 `kind` 当前仅接受 `local` 和 `test`，迁移隔离不能作为第三种可执行环境。写入测试设计时校验环境属于指定系统；生成批次和检查记录必须引用同一设计下的数据。编辑需求会追加递增版本而不覆盖历史版本，生成批次响应中的 `isStale` 表示其来源是否落后于当前需求版本。

测试设计 UI 尚未切换到该 bridge。为保持当前页面在 Task 9 前可用，`loadTestCases` / `saveTestCases` / `upsertTestCase` 暂作为明确标记 deprecated 的 legacy compatibility 入口；`loadLegacyTestCases` 是旧 `test_cases` 的只读迁移入口。Task 9 完成 SQLite UI 接入和迁移验证后删除这些兼容写入口。回归配置中的 `accountCombinationId` 保存前会按当前用户校验账号组合归属。

### 6.7 流程资源 scope 与运行快照

`test_accounts`、`account_combinations`、`workflow_scenarios`、`workflow_runs`、`failure_evidence` 和 `defect_drafts` 已增加 nullable `system_id`、`environment_id` 与 `scope_state`。账号另有 nullable `owner_id`，其语义是 `created_by` 审计信息，不是访问控制字段；运行另有 nullable `design_id`、`requirement_version_id` 和 `snapshot_json`。初始化会按列检测后执行可重复的 `ALTER TABLE ... ADD COLUMN`，再创建 scope 索引，因此新数据库与旧数据库升级后的列集合一致，多次初始化不会重复添加列。

新增写入口为 `create_scoped_test_account`、`save_scoped_account_combination`、`save_scoped_workflow_scenario` 和 `create_scoped_workflow_run`。对应前端 bridge 使用 `ScopeRef { systemId, environmentId }`。这些入口要求显式 scope，并验证环境属于系统。测试账号由管理员维护，在相同 system/environment 内向各执行用户共享；引用时只校验 enabled、role 和 scope，不按账号 `owner_id` 隔离。账号组合、场景、设计和运行仍按当前用户 owner 隔离，跨 owner 引用按 `NOT_FOUND` 拒绝，跨系统或环境引用按 `CROSS_ENVIRONMENT_REFERENCE` 拒绝。证据和缺陷草稿不接受前端指定 scope，而是从已验证的运行记录派生。

scope-aware 运行创建在单个 SQLite 事务中校验引用并写入 `snapshot_json`。`WorkflowRunSnapshot` 固化场景 ID、名称、类型、来源用例、步骤，账号组合 ID、名称、角色账号显示快照，以及关联用例 ID。运行更新输入仅包含状态和当前步骤，不能覆盖 scope 或快照；后续编辑场景、组合或账号不会改变历史运行返回的 `snapshot`。

旧命令暂时保留，仅用于读取、维护或生成明确标记为 `scope_state='legacy'` 的兼容数据；nullable scope 不会被填成虚构的 `local/test`。旧记录在 Task 9 完成用户确认的 system/environment 归类和幂等迁移前保持 legacy/unassigned。Task 9 迁移完成并验证前端已不再调用旧入口后，才可删除这些兼容命令。

回归套件和测试用例的当前 UI 仍临时读取和写入按用户隔离的 legacy localStorage，尚无 SQLite suite 表；这些入口不是新正式设计的推荐保存路径。`suiteId` 只是前端历史数据使用的不透明 ID，本阶段不把它伪装成已具备 scope 的数据库资源，也不校验其引用；Task 9 将套件和用例迁入 SQLite、补齐显式系统/环境归属后，再启用同域校验并删除兼容写入口。

### 6.8 Rust 持久运行管理器

`run_manager.rs` 是新执行生命周期的唯一状态所有者。Tauri 在启动时将一个 `RunManager` 注册为 managed state；前端通过 `start_run`、`pause_run`、`resume_run`、`terminate_run`、`get_run`、`list_runs`、`list_active_runs` 和 `list_run_events` 调用，并通过 `run://updated`、`run://event` 接收广播。`start_run` 在写入 SQLite 后立即返回 run ID，后台 Tokio task 与页面生命周期无关。

状态集合为 `queued`、`preflight`、`running`、`pause_requested`、`paused`、`waiting_handoff`、`passed`、`business_failed`、`blocked`、`cancelled`、`interrupted`；合法转换集中定义在 Rust。暂停请求只在当前原子命令得到终态响应并写入 checkpoint 后落为 `paused`；恢复会重新验证固化计划，再进入 `queued -> preflight -> running`。终止会唤醒正在等待 worker stdout 的任务，关闭 worker stdin 并 kill 未退出子进程。应用启动时没有可恢复 worker 的活动记录统一改为 `interrupted` 并清除 lease/PID。

新表 `execution_runs` 保存 owner、状态、当前步骤、checkpoint、不可变 snapshot、受限 execution plan、脱敏错误类别/消息、时间戳及 worker PID/lease 元数据；`execution_events` 以 `(run_id, sequence)` 为主键，序号和事件在同一 SQLite 事务内生成。该方案不重建旧 `workflow_runs` 的 CHECK 约束，因此升级可回滚且不会破坏旧 UI；`workflow_runs`/`workflow_run_events` 继续作为 legacy 兼容和后续迁移来源，新 API 不再使用 `execution_blocked`，统一为 `blocked`。

execution plan 仅允许 `execute`、`observe`、`act`、`agent`，每条命令必须包含允许 origin 和超时边界，`agent` 另有最大动作数。Rust 在启动前检查命令/动作白名单、字段闭集、URL/origin 及秘密字段或占位符，sidecar 再执行协议级验证；任意脚本不能进入 worker。单个进程内只授予一个浏览器 lease，其余运行保持 `queued`。worker stdout 只接受 NDJSON 协议，stderr 有长度上限并先脱敏；模型 HTML、429、timeout 和 connection 错误有限指数退避后进入 `blocked`，业务断言进入 `business_failed`。

API key 不进入 manager 输入、计划、snapshot、事件或 SQLite；worker adapter 只在启动子进程时从现有 OS keyring 读取并临时注入环境。`waiting_handoff` 已纳入状态机和恢复路径，但当前受限 Stagehand worker 不接受凭据/人工接管命令，实际触发仍属于上层 Rust 登录兼容流边界。

## 7. 构建与发布

### 7.1 工具链

- Node.js `>=22.12 <23`
- Rust stable
- 锁文件：根目录和 `sidecar/` 的 `package-lock.json`、`src-tauri/Cargo.lock`

`scripts/prepare-sidecar.mjs` 执行以下工作：

1. 校验构建平台和 Node 主版本。
2. 复制对应平台 Node Runtime。
3. 复制 sidecar 源码和锁文件。
4. 执行 `npm ci --omit=dev`。
5. 将结果写入未提交的 `src-tauri/resources/`。

### 7.2 产物

- Windows：NSIS `测试小助手_<version>_x64-setup.exe`
- macOS：Apple Silicon `.app` 和 `.dmg`

macOS 当前未配置 Developer ID，不签名、不公证。Windows 安装模式为 current user。

## 8. 安全设计

- API Key 不保存到 localStorage、SQLite、报告或日志。
- 密码仅保存 Argon2id 摘要。
- 数据命令从后端会话获取用户身份。
- 默认数据安全模式为“严格脱敏”。`src/utils/privacy.ts` 在前端统一遮蔽手机号、身份证、邮箱、银行卡、薪资、部门、姓名等敏感文本。
- 需求生成用例、模板生成、Classic LLM、Script 生成、Stagehand 指令和报告写入前均接入脱敏工具。
- Stagehand 底层仍可能在 Agent 运行时读取页面 DOM；涉及真实人事数据时应优先使用虚构测试数据、本地模型或经过脱敏的测试环境。
- WebView 启用 CSP，不再使用 `csp: null`。
- sidecar 发布版只从安装资源定位，不回退到任意项目路径。
- 子进程参数使用 `Command` 参数数组，不拼接 Shell 命令。

## 9. 当前验证状态

已完成：

- ESLint
- TypeScript 编译
- Vite 生产构建
- Rust `cargo check --locked`
- Windows release 与 NSIS 构建
- Windows 静默安装、启动、内置 Node 22 检查和卸载

macOS 代码和脚本已准备，但最终 `.dmg` 必须在 Apple Silicon Mac 上构建和验证。

## 10. 已知限制

- macOS 包未签名，首次启动需要用户手动允许。
- 当前管理员重置密码不会强制用户首次登录修改。
- 当前无登录失败锁定策略。
- 模板与非敏感配置仍使用按用户隔离的 localStorage。
- 测试设计元数据、需求版本、生成批次、检查记录和回归配置已具备 SQLite 后端和 typed Tauri bridge；现有 UI 尚未切换，测试用例和回归套件仍临时使用 deprecated localStorage 兼容入口，Task 9 负责迁移与清理。
- 严格脱敏基于规则匹配，不能保证识别所有自定义敏感字段；人事测试环境仍建议使用虚构数据。
- 报告采用用户专属 JSON，尚未迁移到统一 Repository/SQLite。
- sidecar 生产依赖较大，安装包构建压缩耗时较长。
- 自动化测试覆盖仍有限，目前以构建和安装冒烟测试为主。

## 11. 开发规范

1. 不在前端持久化密钥或密码。
2. 新增 Tauri 命令时同步更新 `generate_handler!`、前端类型和本节接口表。
3. 用户数据必须从后端会话派生 owner，不接受前端自报身份。
4. 跨平台路径使用 `PathBuf` / Tauri path API，不拼接平台分隔符。
5. 修改依赖后更新对应锁文件。
6. 功能、配置、命令、数据格式或发布流程变化时，同一任务内更新 README、开发文档和相关部署文档。
7. 完成前运行 `$maintain-project-docs` 技能规定的一致性检查。
