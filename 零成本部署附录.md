# LogicGuard AI：本地 Ollama 部署附录（可选）

> **推荐方案**：直接使用 **DeepSeek 等云端付费 API**，在 Settings 配置 API Key 即可，无需本文档中的本地算力部署。详见 [开发文档 §4.3.4 / §8.1](./开发文档.md#434-ai-模型适配层)。
>
> 本文档适用于：希望**零 API 费用**、任务较简单、或内网离线环境。本地 7B 模型对 **Stagehand 多步 Agent** 能力有限，复杂场景仍建议 DeepSeek。

## 1. 何时使用本地 Ollama

| 场景 | 建议 |
| --- | --- |
| Stagehand 默认路径、复杂 Web 表单 | **DeepSeek 云端 API** |
| Script 生成（长链路 JSON） | **DeepSeek 云端 API** |
| 简单 Classic 步骤、预算敏感 | 可尝试本地 Ollama |
| 完全离线、无公网 API | 本地 Ollama（接受较低成功率） |

## 2. 部署架构（可选）

「家里一台电脑作算力中心、公司电脑只装客户端」：

| 角色 | 设备 | 运行服务 |
| --- | --- | --- |
| 算力端 | 家用 PC（建议独显 6GB+ 显存） | Ollama、Tailscale |
| 执行端 | 办公电脑 | LogicGuard AI + **仍可用 DeepSeek**（与 Ollama 二选一） |

## 3. 算力端：安装 Ollama

1. 访问 [ollama.com](https://ollama.com) 下载安装
2. 允许远程连接（若需 Tailscale 暴露）：
   ```powershell
   OLLAMA_HOST=0.0.0.0
   ```
3. 下载模型：
   ```bash
   ollama pull qwen2.5:7b    # 约 6GB 显存，简单 Classic 可用
   ```

LogicGuard Settings 配置：

- Provider: `ollama`
- Base URL: `http://127.0.0.1:11434`（远程则填 Funnel 地址 + `/v1`）
- Model: `qwen2.5:7b`

## 4. Tailscale Funnel（远程访问家里 Ollama）

仅在使用**远程家里 Ollama** 时需要；使用 DeepSeek 时**不需要** Tailscale。

### 4.1 暴露 Ollama（端口 11434）

```powershell
tailscale funnel reset
tailscale funnel --bg 11434
```

执行端 Settings：`base_url: https://your-node.ts.net/v1`

### 4.2 关闭 Funnel

```powershell
tailscale funnel --off 11434
```

## 5. PocketBase（规划中）

模板与报告当前存 localStorage，PocketBase 集成尚未完成。若提前部署可参考 [pocketbase.io](https://pocketbase.io/docs/)。

## 6. 验证清单

**DeepSeek 方案（推荐）**

- [ ] Settings → Provider `openai_compat`，Base URL `https://api.deepseek.com`，Model `deepseek-chat`
- [ ] 测试 LLM 连接成功
- [ ] Chrome CDP 已启动，Dashboard Stagehand 任务可执行

**本地 Ollama 方案（可选）**

- [ ] `curl http://127.0.0.1:11434/api/tags` 有模型列表
- [ ] Settings → 测试 LLM 连接成功
- [ ] 简单 Classic 任务可跑通（复杂任务建议切回 DeepSeek）

## 7. 相关文档

- [开发文档.md](./开发文档.md) — DeepSeek 推荐配置 §4.3.4
- [开发文档 §13.2](./开发文档.md#132-常见问题faq) — 故障排查
