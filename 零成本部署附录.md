# 测试小助手零服务器部署附录

本文说明如何在不购买服务器、不部署 PocketBase/Tailscale/Ollama 的情况下安装和维护测试小助手。

> 应用部署本身零服务器费用；云模型是否收费取决于用户选择的 Provider。每个用户使用自己的 API Key，不共享项目密钥。

## 1. 部署结构

```text
Windows / Mac
  └─ 测试小助手
      ├─ React + Tauri
      ├─ 本地账号与报告
      ├─ 本地测试用例与回归套件
      ├─ 系统凭据库中的用户 API Key
      ├─ 内置 Node.js 22 + sidecar
      └─ 本机 Chrome / Edge
             │
             └─ 用户选择的云模型 API
```

不需要长期运行的服务端、数据库、VPN 或家庭算力机器。

## 2. Windows 安装

1. 获取 `测试小助手_<version>_x64-setup.exe`。
2. 双击安装，默认安装到当前 Windows 用户。
3. 首次启动创建管理员。
4. 在“系统设置”中配置当前用户的模型和 API Key。
5. 启动受控浏览器并完成目标系统登录。

安装包已经包含 Node Runtime 和 sidecar。运行端不要再单独安装 `sidecar/node_modules`。

## 3. macOS Apple Silicon 安装

当前版本必须在 Apple Silicon Mac 上执行 `npm run bundle:macos` 生成 `.dmg`。

由于内部版本未签名、未公证：

1. 将应用拖入 Applications。
2. 首次启动在 Finder 中右键应用并选择“打开”。
3. 如仍被拦截，到“系统设置 → 隐私与安全性”允许打开。

如果需要普通用户双击无警告安装，必须申请 Apple Developer 账号并增加 Developer ID 签名与 notarization；这不属于当前零成本方案。

## 4. 本地账号与 API Key

- 首次启动创建管理员。
- 管理员在设置页创建、禁用用户或重置密码。
- 每个用户配置自己的 API Key。
- Windows 密钥保存在 Credential Manager。
- macOS 密钥保存在 Keychain。
- 卸载或升级时不要手工删除系统凭据和应用数据，除非确定不再需要。

本地登录主要防止误用和界面数据串用，不用于抵御拥有系统管理员权限的人员。

## 5. 人事数据安全

- 默认数据安全模式为“严格脱敏”，会在发送给模型和写入报告前遮蔽手机号、身份证、邮箱、银行卡、薪资、部门、姓名等敏感文本。
- 测试设计生成的测试数据默认使用员工A、手机号_1、部门_1 等虚构值。
- 严格脱敏是降低风险的本地规则，不等于合规审计或 DLP。测试环境仍应尽量使用虚构员工数据。
- 如果页面里存在真实员工信息，不建议直接使用云端模型增强模式；优先使用本地/内网模型，或先准备脱敏测试环境。

## 6. 数据位置与升级

应用数据目录包含：

- `logicguard.db`：用户账号与密码摘要
- `logicguard.db` 中的 `execution_runs` / `execution_events`：运行、事件与报告数据源
- `ChromeProfile/`：受控浏览器登录态

API Key 位于操作系统凭据库，不在上述文件中。正式测试设计、测试用例和回归配置位于 SQLite；模板和非敏感 UI 配置仍使用用户专属 localStorage 命名空间。

覆盖升级前建议备份应用数据目录。安装包版本升级不得改变 Tauri identifier `ai.logicguard.desktop`，否则系统会视为另一个应用。

## 7. 验证清单

- [ ] 未安装 Node.js 的电脑可以启动应用
- [ ] 首次启动可以创建管理员并重新登录
- [ ] 管理员可以创建普通用户
- [ ] 两个用户看不到对方模板和报告
- [ ] 两个用户看不到对方测试用例和回归套件
- [ ] 两个用户分别配置 API Key 后连接测试成功
- [ ] 需求或场景模板可以生成正常、边界、空值、权限、重复提交和组合测试用例
- [ ] 已确认用例可以加入回归套件并一键执行
- [ ] 包含手机号、身份证、薪资等内容的需求/报告会被脱敏
- [ ] 受控 Chrome / Edge 可以启动并连接 CDP
- [ ] 任务执行、取消和报告生成正常
- [ ] 应用退出后没有遗留 sidecar 进程
- [ ] 覆盖升级后账号、报告、凭据和 Profile 保留
- [ ] 卸载程序可以正常完成

## 8. 构建

构建机要求 Node 22 和 Rust stable：

```bash
npm ci
npm run check
```

Windows：

```powershell
npm run bundle:windows
```

macOS Apple Silicon：

```bash
npm run bundle:macos
```

详细产物路径和发布步骤见 [BUILDING.md](./BUILDING.md)。技术实现见 [开发文档.md](./开发文档.md)。
